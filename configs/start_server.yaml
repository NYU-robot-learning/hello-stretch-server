defaults:
  - _self_
  - network: network
  - camera: d405
  - controller: position
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled

use_depth: False

hydra:  
  output_subdir: null  
  run:  
    dir: .
  